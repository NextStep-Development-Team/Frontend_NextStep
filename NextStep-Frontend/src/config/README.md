# Configuración Global

Constantes y variables de entorno.

## Contenido

- `env.js`: Validación y exportación de variables de entorno (VITE_API_URL).
- `constants.js`: Valores fijos (roles, estados de documentos).
