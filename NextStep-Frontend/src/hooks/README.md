# Hooks Globales

Costum hooks que no pertenecen a una feature específica pero son útiles en toda la aplicación.

## Ejemplos

- `useForm.js`: Lógica de formularios.
- `useLocalStorage.js`: Persistencia.
- `useWindowSize.js`: UI Reactivo.
